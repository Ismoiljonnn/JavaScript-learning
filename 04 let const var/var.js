// var – the keyword used to declare variables prior to ES6.
// It performed almost the same function as let.

var age = 23;
age = 24;

// Although let and var are similar, they have key differences.
// A variable declared using var is function-scoped, whereas let
// is block-scoped.

// In JavaScript, it is possible to create a variable without
// specifying any keyword, but creating variables in this manner
// is not recommended.

user = Ismoiljon; // It is not recommended.
console.log(user);